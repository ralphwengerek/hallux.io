import React from 'react';
import { render } from '../../utils/testUtils';
import { Footer } from './Footer';

describe('<Footer />', () => {
  it('renders', () => {
    render(<Footer />);
  });
});
